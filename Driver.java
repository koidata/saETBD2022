/** TODO
 *  Create Parent Nodes
 *  - Auto fill genotype of each Node
 *  Population class that will take a Node data type and generate a user specified amount of Parents (Nodes)
 *  - Population will also take in a user specified number of generations 
 *  - Population will load each Parent Node, in a random order, into an ArrayDeque to be processed later when doing BitWise recombo
 *      - Each generation processes the bitwise recombonation, but after the first initial ArrayDeque load-in, the Parent Nodes are-
 *        loaded back into the data struct. in the same order
 *  
 * 
 */

import java.security.PKCS12Attribute;
import java.util.*;

public class Driver {
    //generate parent1 and parent2 
    public static void main(String[] args) {

    }
}
